import {nextPlugin} from '@genkit-ai/next';

/**
 * This is the Next.js API route handler for Genkit.
 * It's a mandatory file for the Genkit Next.js plugin to work.
 */
export const {GET, POST} = nextPlugin();
